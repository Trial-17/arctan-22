# Nom du workflow qui apparaîtra dans l'onglet "Actions" de votre repo
name: Build macOS AES

on:
  # push:
  #   branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

env:
  PRODUCT_VERSION: "2.0" # Changez ici pour passer à la v2.0, etc.
  STABLE_TAG: "premiere-mac-aes-latest"

jobs:
  build-macos:
    strategy:
      matrix:
        include:
          - os: macos-15
            platform: Mac-ARM
          - os: macos-15
            platform: Mac-Intel

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Import and Verify Apple Installer Certificate
        env:
          INSTALLER_CERT_P12: ${{ secrets.APPLE_INSTALLER_CERTIFICATE_P12 }}
          INSTALLER_CERT_PASSWORD: ${{ secrets.APPLE_INSTALLER_CERTIFICATE_PASSWORD }}
        run: |
          security create-keychain -p temp build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p temp build.keychain
          echo "$INSTALLER_CERT_P12" | base64 --decode > installer_cert.p12
          security import installer_cert.p12 -k build.keychain -P "$INSTALLER_CERT_PASSWORD" -A -T /usr/bin/productsign -T /usr/bin/pkgbuild
          security set-key-partition-list -S apple-tool:,apple: -s -k temp build.keychain

          echo "--- Available Identities ---"
          security find-identity -v build.keychain
          echo "------------------------------------"

          INSTALLER_IDENTITY=$(security find-identity -v build.keychain | grep "Developer ID Installer")
          if [ -z "$INSTALLER_IDENTITY" ]; then
            echo "::error::Developer ID Installer certificate not found."
            exit 1
          fi

          echo "✅ Installer certificate found in the keychain."

      # Créer et signer le ZXP (robuste, avec certificat auto-signé)
      - name: Create and Sign ZXP
        working-directory: ./frontend
        run: |
          echo "--- Making ZXPSignCmd executable ---"
          chmod +x ./ZXPSignCmd

          echo "--- Generating self-signed certificate for ZXP ---"
          ./ZXPSignCmd -selfSignedCert "FR" "France" "PremiereCopilot" "PremiereGPTaescripts" "plugin2025" "PremiereGPTBeta.p12" \
            -email "admin@premierecopilot.com" \
            -orgUnit "PremiereGPTaescripts" \
            -validityDays 3650

          if [ ! -f "PremiereGPTBeta.p12" ]; then
            echo "::error::Failed to generate self-signed certificate."
            exit 1
          fi

          echo "--- Signing the extension folder into ZXP ---"
          ./ZXPSignCmd -sign "./PremiereGPTaescripts" "./PremiereGPTaescripts.zxp" "./PremiereGPTBeta.p12" "plugin2025"

          if [ ! -f "PremiereGPTaescripts.zxp" ]; then
            echo "::error::Failed to create ZXP."
            exit 1
          fi

          echo "--- Verifying ZXP signature ---"
          ./ZXPSignCmd -verify "./PremiereGPTaescripts.zxp" -certinfo

      - name: Build and Sign PKG Installer
        run: |
          mkdir -p build/pkg_root/PremiereGPTaescripts
          unzip frontend/PremiereGPTaescripts.zxp -d build/pkg_root/PremiereGPTaescripts
          mkdir -p build/scripts
          chmod +x preinstall_aes.sh postinstall_aes.sh
          cp preinstall_aes.sh build/scripts/preinstall
          cp postinstall_aes.sh build/scripts/postinstall

          echo "--- Available identities in build.keychain ---"
          security find-identity -v build.keychain
          echo "---------------------------------------------"

          SIGNING_IDENTITY=$(security find-identity -v build.keychain | grep "Developer ID Installer" | head -n 1 | sed -e 's/.*"\(.*\)"/\1/')

          if [ -z "$SIGNING_IDENTITY" ]; then
            echo "::error::Developer ID Installer identity not found."
            exit 1
          fi

          echo "Using Installer signing identity: $SIGNING_IDENTITY"
          pkgbuild --root build/pkg_root \
                   --install-location "/Library/Application Support/Adobe/CEP/extensions" \
                   --scripts build/scripts \
                   --identifier com.trial17.premierecopilot \
                   --version 1.0 \
                   --sign "$SIGNING_IDENTITY" \
                   build/PremiereCopilot-Installer.pkg

      - name: Notarize and Staple PKG
        run: |
          xcrun notarytool store-credentials "notary-profile" \
            --apple-id "${{ secrets.APPLE_ID }}" \
            --team-id "${{ secrets.APPLE_TEAM_ID }}" \
            --password "${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}"

          echo "--- Submitting to Apple Notary Service ---"
          xcrun notarytool submit build/PremiereCopilot-Installer.pkg \
            --keychain-profile "notary-profile" \
            --wait

          echo "--- Stapling Ticket ---"
          xcrun stapler staple build/PremiereCopilot-Installer.pkg

      - name: Create Release ZIP
        run: |
          zip -j build/PremiereCopilot-Installer-${{ matrix.platform }}-aescripts.zip build/PremiereCopilot-Installer.pkg Quickstart.pdf

      - name: Upload Installer ZIP
        uses: actions/upload-artifact@v4
        with:
          name: PremiereCopilot-Installer-${{ matrix.platform }}-aescripts
          path: build/PremiereCopilot-Installer-${{ matrix.platform }}-aescripts.zip

      - name: Update Latest Release (Lien Stable)
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main'
        with:
          tag_name: ${{ env.STABLE_TAG }}
          name: Latest Stable Version (MacOS AES)
          files: build/PremiereCopilot-Installer-${{ matrix.platform }}-aescripts.zip
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Versioned Release (Historique)
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main'
        with:
          tag_name: v${{ env.PRODUCT_VERSION }}.${{ github.run_number }}-mac-aes-${{ matrix.platform }}
          name: Version ${{ env.PRODUCT_VERSION }}.${{ github.run_number }} (MacOS AES - ${{ matrix.platform }})
          files: build/PremiereCopilot-Installer-${{ matrix.platform }}-aescripts.zip
          generate_release_notes: true
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
